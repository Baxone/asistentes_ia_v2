Contexto:
    Necesito crear un componente llamado ProductCard.
    Debe mostrar: imagen, nombre, precio, categoría y un botón "Añadir al carrito".

Instrucción:
    Crear un componente de React 19 con Vite y Tailwind CSS.

Role:
    actua como un experto en React 19 con Vite y Tailwind CSS.
  

Restricciones:
   Usa CSS Modules para los estilos. Incluye las props tipadas con una interfaz.
    El botón debe llamar a una función onAddToCart que recibe el id del producto y la cantidad.

Formato:
    tsx - quiero trabajar con typescript
